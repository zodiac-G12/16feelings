import { CheckIcon } from "@/components/parts/icons";

export const LastTop = () => {
  return (
    <div class="flex justify-center">
      <div class="flex items-center justify-center px-6 py-3 rounded-full bg-white/10 gap-1">
        <CheckIcon />
        <span>診断完了</span>
      </div>
    </div>
  );
};
